<p id="notice"><%= notice %></p>

<p>
  <strong>Название поезда:</strong>
  <%= @train.title %>
</p>

<p>
  <strong>Находится на станции:</strong>
  <% if @train.current_station.present? %>
    <%= @train.current_station.title %>
  <% else %>
    Not set
  <% end %>
</p>

<p>
  <strong>Входит в маршрут:</strong>
  <% if @train.route.present? %>
    <%= @train.route.title %>
  <% else %>
    Не задан
  <% end %>
</p>

<p>
  <strong>Вагоны</strong>
  <ul>
    <% @train.vagons.each do |car| %>
        <li><%= link_to "Вагон №#{car.number}", car %></li>
    <% end %>
  </ul>
</p>

<p>
  <%= link_to "Добавить вагон", new_train_vagon_path(@train) %> |
  <%= link_to "Сортировать по порядку", new_train_vagon_path(@train) %> |
  <%= link_to "Сортировать в обратном порядке", new_train_vagon_path(@train) %>
</p>

<%= link_to 'Редактировать', edit_train_path(@train) %> |
<%= link_to 'Назад', trains_path %>
